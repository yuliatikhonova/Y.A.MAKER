import React from "react";
import "./style.css";

function Gallery(props) {
    return (
        // gallery layout tags here, will display gallery data using {props.data} data being each piece like props.img
    );
}

export default Gallery;